{
  "name": "sugar-mama-signup",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker that handles email signups + contact messages for the Sugar Mama Sweets website.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
